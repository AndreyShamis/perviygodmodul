/*
 * Ex8b
 * =============================================================
 * Writen by: Yosi Cohen, id: 333444555, login:yosico
 *
 * Here comes the main documentation: What the program does,
 * and how (not too long!)
 * A program that reads and sorts a list of numbers, using selection sort.
 * input: list of numbers
 * output: same numbers sorted
 * algorithm: selection sort
 */

//-------------- include section --------------
#include <iostream>

//-------------- using section --------------
using  std::cin ;
using  std::cout ;
using  std::endl ;

//-------------- const and enum section --------------
const ARR_SIZE = 10 ;

//-------------- main --------------
int main() {
    // _EACH_ variable should be documented!
	int arr[ARR_SIZE];                 // for input and sort
	int round;                         // for iterating on arr
	.
	.
	.


	return(0);
}
